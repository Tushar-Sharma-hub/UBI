[build]
  base = "client"
  command = "npm ci && npm run build"
  publish = "build"

# SPA fallback to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


